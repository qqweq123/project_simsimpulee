---
title: "Architecture & System Foundation Index"
author: "Total Director (Senior Engineer) & Director of Data Architecture"
last_updated: "2026-02-28"
status: "Active"
---

<!-- 파일 기능 서술 -->
> **문서 기능 (Document Function):** 본 파일(`02_architecture/README.md`)은 Mellow Wave 프로젝트의 프론트엔드/백엔드 아키텍처, 데이터베이스 스키마 명세, 네트워크 토폴로지 및 보안 규약을 통제하기 위한 **시스템 아키텍처 최상위 지침서**입니다.

# 02_architecture (시스템 파운데이션 / 보안 / DB)

**site URL (www.mellowave.me)**

이 디렉토리는 시스템 아키텍처, 데이터 거버넌스, 인프라스트럭처 최적화 전략 등 Mellow Wave 아키텍처의 뼈대를 보관합니다. 가장 진보적이고 깨끗한 클린 아키텍처를 지향하며, 총괄 수석 엔지니어(`@general-engineer`)와 데이터 통합 처리 총괄 디렉터(`@dataarchitecturedirector`)의 검증을 거친 마스터 규약만이 기록됩니다.

## 📂 하위 디렉토리 구조 및 역할

### `database/` (Data Architecture & Storage)
- **Supabase-Postgres Best Practices**에 입각한 DB 스키마 설계도 및 마이그레이션 이력이 저장됩니다.
- **SSOT(유일한 진실의 공급원)** 기반의 마스터 데이터 관리(MDM) 명세가 포함됩니다.

### `frontend/` (FSD Architecture)
- Feature-Sliced Design(FSD) 아키텍처 컨벤션, 상태 관리(State Management) 규칙, Vite 빌드/번들링 최적화 명세를 정의합니다.

### `backend/` / `edge_functions/` (Serverless Compute)
- Supabase Edge Functions, 외부 API 통합 연동 명세서, 성능 튜닝(Cold/Hot Start) 관련 정책을 다룹니다.

### `security/` (Data Governance & Security)
- 개인정보(PII) 비식별화, 컴플라이언스 대응, CORS/CSP 정책 및 RLS(Row-Level Security) 권한/정책(Policies) 명세를 기록합니다.

---

## ⚙️ 아키텍처 설계 및 문서화 통합 규정 (Architecture Rules)

### 1. 데이터 파이프라인 및 거버넌스 엄수 (@dataarchitecturedirector)
- **ETL/ELT 파이프라인**: 서비스에서 발생하는 사용자 행동/테스트 데이터가 어떻게 추출되고 변환되어 적재되는지 그 흐름(Data Lineage)을 아키텍처 다이어그램(Mermaid 등)으로 필수 명시해야 합니다.
- **데이터 품질 및 리소스 최적화**: 
  - 불필요한 비용 발생을 막기 위해 콜드 버킷(Cold Storage) 등 계층적 데이터 저장 모델 설계를 고려해야 합니다.

### 2. Supabase-Postgres 고도화 최적화 (Postgres Best Practices)
아키텍처 설계 시 **반드시** 다음의 Supabase 가이드를 준수하고 문서에 명세할 것:
- **`[query-*]` Critical**: 쿼리 페이로드 최소화. 외래키 및 자주 조회되는 조건에 대한 **인덱스 또는 부분 인덱스(Partial Indexing)** 설계 의도를 명확히 적시할 것 (`CREATE INDEX ... WHERE ...`).
- **`[conn-*]` Critical**: 서버리스 함수(Edge Functions) 등에서 다발적 연결 발생을 대비해 **Connection Pooling(Supavisor)** 적용 및 최적화 설정을 명세할 것.
- **`[security-*]` Critical**: 모든 공개 테이블 접근에는 **RLS(Row Level Security)** 정책 코드가 동반되어야 하며, 권한 구조를 문서화할 것.
- **`[schema-*]` High**: 정규화 원칙 준수, 적절한 자료형 사용(예: Timestamp vs Timestamptz 등) 여부를 리뷰 및 기록할 것.

---
#### 4. Chapter별 기록 사항
- **[수정 이력]**:
  - `2026-02-28`: `02_architecture` 하위 폴더들에 대한 총괄 지침 작성. Supabase 모범 사례 및 데이터 품질 파이프라인 관리(MDS/거버넌스) 룰 추가.
- **[변경 사유 및 철학]**: 시스템의 토대는 단순히 코드가 아니라 완벽한 도식과 철학에서 시작됨. 구조에서 병목(Bottleneck)이나 보안 구멍이 발생하지 않도록, 최고 수준의 데이터베이스 및 시스템 엔지니어링 패러다임을 강제함.
- **[구현 방향성]**: 이 디렉토리에 추가되는 모든 인프라/보안/DB 명세 문서는 반드시 상위 규정에 어긋남이 없는지, 쿼리 비효율이 없는지 (EXPLAIN 분석 결과 첨부 등) 검증된 후 반영되어야 함.

> _"우리의 인프라는 트래픽의 급증에도 굳건해야 하며, 쿼리는 단 1ms의 낭비도 없어야 합니다. 정밀하게 조율된 데이터 파이프라인이야말로 진정한 엔지니어링의 정점입니다." - 총괄 수석 엔지니어 (Total Director)_
